*{box-sizing:border-box}
:root{--purple:#80005c;--dark:#650047;--bg:#f7f5f7;--text:#29272c;--muted:#6e6a70;--border:#d7d1d8}
body{margin:0;background:var(--bg);color:var(--text);font-family:Arial,Helvetica,sans-serif}
.topbar{background:var(--purple);color:white;padding:28px 22px 24px;box-shadow:0 3px 12px #0002}
.brand{font-size:34px;font-weight:700}.subtitle{margin-top:8px;font-size:17px;opacity:.9}
.container{width:min(720px,100%);margin:auto;padding:28px 20px 50px}
.screen{display:none}.screen.active{display:block}
h1,h2{margin:0}.intro{color:var(--muted);font-size:18px;margin:8px 0 28px}
.menu-grid{display:grid;gap:16px}
.menu-card{width:100%;min-height:145px;padding:24px;border:1px solid var(--border);border-radius:20px;background:white;text-align:left;cursor:pointer;box-shadow:0 4px 16px #0001}
.menu-card.primary{background:var(--purple);color:white;border-color:var(--purple)}
.menu-card b{display:block;font-size:24px}.menu-card span{display:block;margin-top:8px;opacity:.8}
.stats-card{margin-top:22px;padding:20px;background:white;border:1px solid var(--border);border-radius:18px}
.stats-card b{display:block;color:var(--purple);text-transform:uppercase;font-size:13px}.stats-card strong{font-size:42px}
.section-header{display:flex;align-items:center;gap:12px;margin-bottom:24px}
.section-header p{margin:5px 0;color:var(--muted)}
.back{width:44px;height:44px;border:0;border-radius:50%;background:#f4eaf2;color:var(--purple);font-size:24px}
label{display:block;margin:0 0 7px 3px;font-size:14px;font-weight:700}
label em{font-weight:400;color:var(--muted)}
input,textarea{width:100%;padding:14px;border:1px solid var(--border);border-radius:12px;background:white;font:inherit;font-size:16px;outline:none;margin-bottom:17px}
input:focus,textarea:focus{border-color:var(--purple);box-shadow:0 0 0 3px #80005c1a}
.row{display:grid;grid-template-columns:1fr 1fr;gap:14px}.auto{position:relative}
.auto>input{margin-bottom:17px}
.auto>div{display:none;position:absolute;z-index:5;left:0;right:0;top:47px;background:white;border:1px solid var(--border);border-radius:0 0 12px 12px;box-shadow:0 8px 20px #0002}
.auto>div.show{display:block}
.option{padding:12px 14px;border-bottom:1px solid #eee;cursor:pointer}.option:hover{background:#f4eaf2}
.option b{display:block}.option small{color:var(--muted)}
.submit,.cancel{width:100%;min-height:54px;border-radius:28px;font:inherit;font-weight:700;cursor:pointer}
.submit{border:0;background:var(--purple);color:white}.submit:hover{background:var(--dark)}
.cancel{margin-top:12px;border:1px solid var(--border);background:transparent;color:var(--text)}
.card{background:white;border:1px solid var(--border);border-radius:18px;padding:18px;margin-bottom:14px}
.card .title{font-size:21px;font-weight:700;color:var(--purple)}.route{margin-top:7px;font-size:17px;font-weight:600}.meta{margin-top:8px;color:var(--muted);font-size:14px}
.empty{text-align:center;color:var(--muted);padding:35px}
@media(max-width:520px){.container{padding:22px 15px}.row{grid-template-columns:1fr}}
